[project]
name = "prevently-frontend"
version = "0.1.0"
description = "Desktop frontend for the Prevently application"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "requests>=2.34.2",
]


[project.scripts]
frontend = "__main__:main"

[dependency-groups]
dev = [
    "pyinstaller>=6.22.2",
]
